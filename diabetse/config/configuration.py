from diabetse.constants import *
import os, sys

DATASET_PATH = os.path.join(ROOT_DIR, DATA_DIR, DATASET)

RAW_FILE_PATH = os.path.join(
    ROOT_DIR, ARTIFACT_DIR, DATA_INGESTION, RAW_DATA_DIR, RAW_DATA
)
TRAIN_FILE_PATH = os.path.join(
    ROOT_DIR, ARTIFACT_DIR, DATA_INGESTION, INGESTED_DATA_DIR, TRAIN_DATA
)
TEST_FILE_PATH = os.path.join(
    ROOT_DIR, ARTIFACT_DIR, DATA_INGESTION, INGESTED_DATA_DIR, TEST_DATA
)

# TRANSFORMATION CODE

PREPROCESSING_FILE = os.path.join(
    ROOT_DIR, ARTIFACT_DIR, DATA_TRANSFORMATION, PREPROCESS_DIR, PROCESS_DATA
)
TRANSFORM_TEST_PATH = os.path.join(
    ROOT_DIR, ARTIFACT_DIR, DATA_TRANSFORMATION, TRANSFORMATION_DIR, TRANSFORM_TEST_DATA
)
TRANSFORM_TRAIN_PATH = os.path.join(
    ROOT_DIR,
    ARTIFACT_DIR,
    DATA_TRANSFORMATION,
    TRANSFORMATION_DIR,
    TRANSFORM_TRAIN_DATA,
)
FEATURE_ENGI_FILE = os.path.join(
    ROOT_DIR, ARTIFACT_DIR, DATA_TRANSFORMATION, PREPROCESS_DIR, "feature_engi.pkl"
)


# Training code

MODEL_FILE_PATH = os.path.join(ROOT_DIR, ARTIFACT_DIR, MODEL_TRAINER_DIR, MODEL_OBJECT)
